[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "claude_webapi"
version = "1.0.0"
description = "Async Python wrapper for the Claude.ai web app"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["claude", "anthropic", "ai", "wrapper", "async"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "aiohttp>=3.9",
]

[project.optional-dependencies]
dev = ["pytest>=7", "pytest-asyncio>=0.23", "black", "ruff"]

[project.urls]
Homepage   = "https://github.com/cyber-wojtek/Claude-API"
Issues     = "https://github.com/cyber-wojtek/Claude-API/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["claude_webapi*"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
asyncio_mode = "auto"

